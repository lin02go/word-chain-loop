# 《词环》闯关模式产品设计书 v2.0

> 状态：100 关版本已实现并进入持续维护。
>
> 产品名称：词环 / Word Loop
>
> 模式划分：现有玩法正式定义为「休闲模式」，新增独立的「闯关模式」。

## 1. 产品目标

闯关模式要解决休闲模式缺少长期目标的问题。玩家不再每次随机开局，而是按照设计好的关卡顺序逐步学习规则、提高词汇和规划能力，并获得明确的完成反馈。

它不应变成带金币、体力和付费道具的重度手游系统。核心仍然是纯粹的英文单词接环，闯关结构只负责提供顺序、目标和进度。

### 核心体验

1. 玩家进入一关，看到固定的起始词和清楚的过关目标。
2. 每次输入仍遵循当前游戏的两字母衔接规则。
3. 在最大步数内闭合词环即可过关。
4. 少用词、不使用提示可以获得更高评价。
5. 过关后解锁下一关，随时可以重玩已完成关卡。

### 非目标

- 首版不做账号、联网和跨设备同步。
- 首版不做全球排行榜或好友系统。
- 首版不做金币、商城、体力、广告复活。
- 首版不为每关规定唯一答案；同一关应允许多条合理路线。

## 2. 两种模式的正式定义

| 项目 | 休闲模式（现有模式） | 闯关模式（新增） |
|---|---|---|
| 开局方式 | 系统随机选择合理起始词 | 从固定关卡列表选择 |
| 难度 | 玩家选择简单、标准、困难 | 由关卡配置决定并逐步提升 |
| 目标 | 尽可能少地完成随机词环 | 完成指定关卡并解锁下一关 |
| 提示 | 按难度提供 | 每关配置提示次数 |
| 答案 | 可直接查看，查看后记为练习 | 默认过关前不显示完整答案 |
| 纪录 | 按难度和起始词记录最佳长度 | 记录关卡星级、最佳步数和提示次数 |
| 进度 | 没有长期进度 | 有关卡解锁和总星数 |
| 适合场景 | 随开随玩、自由练习 | 持续挑战、学习规则、建立目标感 |

两种模式共用同一个接词引擎、词典和判词规则，但使用不同的开局、结算和存档模块。切换模式不会删除另一模式的纪录。

## 3. 闯关模式的核心规则

### 3.1 关卡组成

每关至少包含以下数据：

- 关卡编号与名称。
- 固定起始词。
- 开放词库等级：常用、标准或扩展。
- 最短可解步数，由系统校验生成。
- 建议步数，用于星级评价。
- 最大步数，超过后判定本次挑战失败。
- 可用提示次数。
- 至少两条经过质量筛选的可行路线。
- 至少两个常见、规范的闭环收尾词。

### 3.2 过关条件

玩家在最大步数内，让最后一个词的末两个字母回到起始词的前两个字母，即可过关。

输入无效单词、重复词、同词根变形、前缀不符或会导致死路时，沿用现有判定逻辑：拒绝该输入，但不消耗步数。

### 3.3 星级评价

- 1 星：在最大步数内完成。
- 2 星：在建议步数内完成。
- 3 星：在建议步数内完成，并且没有使用提示或撤销。

星级只用于重复挑战和总进度展示，不影响下一关的基本解锁。这样较弱的玩家也能继续推进，不会被“必须满星”卡住。

### 3.4 失败与重试

达到最大步数仍未闭环时，本次挑战失败。失败页面提供：

- 重新挑战本关。
- 撤销最后一步后继续，计为辅助通关。
- 查看一个可用提示后继续，计为辅助通关。
- 返回关卡列表。

不设置生命值，也不限制重试次数。

## 4. 关卡结构

### 4.1 当前规模

当前版本包含 100 关，采用连续关卡列表，不设置章节。原有 12 关保持编号和内容不变，以兼容历史存档；扩展关卡按简单、标准、困难三个梯度继续推进。

### 4.2 解锁规则

- 默认解锁第 1 关。
- 完成当前关后，立即解锁下一关。
- 已完成关卡可以随时重玩。
- 星数不作为主线解锁门槛。

### 4.3 关卡质量门槛

每个关卡上线前必须自动验证：

1. 起始词本身属于合适的常用度层级。
2. 起始两字母至少存在两个常见规范收尾词。
3. 至少存在两条不同的合理通关路线。
4. 最短路线中不依赖人名、缩写、敏感词或异常词形。
5. 提示系统能够在每一步给出至少一个不会进入死路的词。
6. 词典升级后重新运行验证，失效关卡不能进入正式关卡表。

## 5. 页面与流程

### 5.1 首页模式入口

在游戏顶部增加模式切换：

- 休闲模式
- 闯关模式

首次进入仍默认休闲模式，避免改变现有玩家习惯。系统记住玩家上次使用的模式。

### 5.2 闯关首页

包含：

- 已完成关卡数与总星数。
- 连续的 100 关关卡列表。
- 每关的锁定、可挑战、已完成状态。
- 已获得星级与最佳步数。
- “继续闯关”按钮，直接进入最近未完成的一关。
- 总进度，例如“完成 48 / 100”。

### 5.3 关卡开始页

显示：

- 关卡编号和名称。
- 起始词。
- 本关最大步数。
- 可用提示次数。
- 本关新规则说明；没有新规则时不显示说明。
- “开始挑战”按钮。

### 5.4 关卡内界面

复用当前游戏主界面，变化部分包括：

- 顶部显示“第 N 关”。
- 难度按钮改为不可编辑的本关难度标签。
- 显示“当前步数 / 最大步数”。
- 显示本关剩余提示次数。
- “新游戏”改为“重新挑战”。
- 增加“返回选关”。
- 过关前隐藏“完整答案”。

### 5.5 结算页

显示：

- 获得的 1–3 星。
- 本次步数与关卡建议步数。
- 是否使用提示。
- 本关最佳纪录是否刷新。
- “下一关”“再玩一次”“返回选关”。

完成第 100 关后，主按钮改为“返回选关”。

## 6. 提示系统

首版提示规则：

1. 方向提示：显示当前必须使用的两个首字母。该信息现在已经存在，不消耗提示次数。
2. 单词提示：每次只显示一个不会进入死路、且在当前接词位置尚未展示过的单词；成功显示后消耗 1 次提示。
3. 在同一接词位置用完所有候选词时，提示“没有更多未显示的提示词”，不消耗次数；进入下一步后使用新的候选池。
4. 使用任意单词提示后，本关最高只能获得 2 星。

首版闯关模式不显示完整答案；休闲模式仍保留“显示答案 · 练习模式”。后续如需增加放弃查看答案，应作为新的独立功能评估，不与失败次数绑定。

## 7. 存档设计

首版使用 `localStorage`，与现有休闲纪录分开保存。

建议存档结构：

```json
{
  "version": 1,
  "unlockedLevel": 8,
  "lastPlayedLevel": 7,
  "levels": {
    "1": {
      "completed": true,
      "stars": 3,
      "bestMoves": 4,
      "bestHintUses": 0,
      "attempts": 2
    }
  }
}
```

要求：

- 存档带版本号，便于以后迁移。
- 关卡配置更新后，不清空已完成进度。
- 存档损坏时回退为空存档，而不是导致游戏无法启动。
- 提供“重置闯关进度”，执行前二次确认。

## 8. 关卡数据结构

```js
{
  id: 1,
  startWord: "algebra",
  maxTier: 0,
  parMoves: 4,
  maxMoves: 7,
  hintLimit: 2,
  minimumSolutions: 2,
  minimumQualityClosers: 3,
  lessonKey: "lesson_two_letter_link"
}
```

关卡表只保存设计意图，不手写完整答案。构建或启动时由现有图搜索引擎计算并验证可行路线，避免词典更新后答案表失效。

## 9. 功能清单——请按编号删减

优先级含义：

- P0：闯关模式成立所必需。
- P1：明显改善体验，建议首版保留。
- P2：可以后续增加。
- P3：不建议首版制作。

| 编号 | 功能 | 优先级 | 建议 | 依赖 | 删除后的影响 |
|---|---|---:|---|---|---|
| F01 | 休闲 / 闯关模式切换 | P0 | 保留 | 无 | 无法进入独立闯关模式 |
| F02 | 固定关卡配置表 | P0 | 保留 | 现有词图引擎 | 无法设计稳定关卡 |
| F03 | 顺序解锁下一关 | P0 | 保留 | F02、F05 | 闯关退化为普通选题列表 |
| F04 | 关卡列表 | P0 | 保留 | F02 | 玩家无法选关或查看进度 |
| F05 | 独立闯关存档 | P0 | 保留 | localStorage | 刷新后进度丢失 |
| F06 | 最大步数与失败条件 | P0 | 保留 | F02 | 关卡缺少明确挑战边界 |
| F07 | 关卡结算页 | P0 | 保留 | F03、F05 | 缺少通关反馈和下一关入口 |
| F08 | 关卡自动质量验证 | P0 | 保留 | 词图引擎 | 容易出现无解或依赖生僻词的关卡 |
| F09 | 关内重新挑战 | P0 | 保留 | F02 | 输错后只能退回选关 |
| F10 | 中英文闯关文案 | P0 | 保留 | 现有语言系统 | 英文界面会出现中文残留 |
| F11 | 当前步数 / 最大步数显示 | P1 | 保留 | F06 | 玩家不知道离失败还有多远 |
| F12 | 继续闯关按钮 | P1 | 保留 | F03、F05 | 每次都要手动寻找下一关 |
| F13 | 三星评价 | P1 | 保留 | F06、F07 | 重玩动力下降，但不影响基本闯关 |
| F14 | 每关最佳步数 | P1 | 建议保留 | F05、F13 | 无法展示个人进步 |
| F15 | 每关提示次数限制 | P1 | 建议保留 | 现有提示系统 | 各关挑战强度不易控制 |
| F16 | 通关前隐藏完整答案 | P1 | 建议保留 | 现有答案系统 | 玩家可能一键跳过挑战 |
| F17 | 章节划分 | P1 | 删除 | F02、F04 | 100 关仍采用连续列表，避免增加额外状态 |
| F18 | 新规则教学卡 | P1 | 可删 | F02 | 新玩家主要靠规则说明自行理解 |
| F19 | 失败页与继续选项 | P1 | 建议保留 | F06 | 失败后只能直接重置，体验生硬 |
| F20 | 返回选关 | P1 | 保留 | F04 | 关内退出不方便 |
| F21 | 撤销最后一步 | P2 | 保留 | 需新增历史状态 | 使用后本关最高 2 星 |
| F22 | 失败三次后开放答案 | P2 | 删除 | F16、失败次数记录 | 首版不提供失败次数答案兜底 |
| F23 | 章节完成庆祝页 | P2 | 删除 | F17 | 已删除章节体系 |
| F24 | 总星数 | P2 | 保留 | F13 | 不影响逐关推进 |
| F25 | 成就系统 | P2 | 保留 | F05 | 包含闭环、最短路线与 100 关通关成就 |
| F26 | 每日闯关任务 | P2 | 建议后续 | 日期与关卡选择 | 不影响主线闯关 |
| F27 | 关卡搜索 / 筛选 | P3 | 暂不实现 | F04 | “继续闯关”覆盖主流程，保持界面轻量 |
| F28 | 关卡地图动画 | P3 | 删除 | F04、额外视觉开发 | 改用清楚的连续关卡列表即可 |
| F29 | 金币与道具商店 | P3 | 删除 | 经济系统 | 不影响核心玩法，反而增加复杂度 |
| F30 | 生命值 / 体力 | P3 | 删除 | 计时与经济系统 | 玩家可以无限重试，更友好 |
| F31 | 排行榜 | P3 | 删除 | 账号、服务器、反作弊 | 首版开发成本显著增加 |
| F32 | 云存档 | P3 | 保留 | 邮箱账户、Pages Functions、D1 | 支持跨设备保存进度 |
| F33 | 剧情人物与对话 | P3 | 删除 | 大量内容与美术 | 主题更纯粹，减少内容成本 |
| F34 | 分享通关结果 | P2 | 可删 | 图片或文本生成 | 不影响单人体验 |
| F35 | 重置闯关进度 | P1 | 保留 | F05 | 测试或重新游玩不方便 |

## 10. 推荐的最小可用版本

如果希望尽快上线，建议只开发以下 17 项：

`F01–F12、F14–F16、F20、F35`

即：模式切换、固定关卡、顺序解锁、关卡列表、本地进度、步数限制、结算、自动校验、重试、双语、步数显示、继续闯关、最佳步数、提示限制、隐藏答案、返回选关和重置进度。

当前首版在上述基础上额外保留三星、失败页、撤销和总星数；删除章节、失败三次后开放答案、成就、分享及所有联网功能。

## 11. 推荐完整版范围

保留所有 P0、P1 功能，再加入：

- F21 撤销最后一步。
- F24 总星数。
- F34 分享通关结果。

仍然不建议加入金币、体力、商城、排行榜、剧情和云存档。

## 12. 开发结构建议

建议拆成以下模块，保持闯关系统独立：

```text
共享层
├─ DictionaryService        词典与词级别
├─ WordGraphEngine          路径、死路和闭环判定
└─ WordValidationService    单词、前缀、重复词根判定

休闲模式
├─ CasualGameController     随机开局、难度与提示
└─ CasualRecordStore        当前最佳纪录

闯关模式
├─ CampaignController       关卡开始、失败、通关
├─ LevelCatalog             固定关卡配置
├─ LevelValidator           上线前质量校验
├─ CampaignProgressStore    解锁、星级、最佳步数
└─ CampaignUI               选关、关内状态、结算
```

即使项目继续保持单个 HTML 文件，代码内部也应按以上职责分区，避免闯关逻辑塞进现有 `WordChainGame` 后越来越难维护。

## 13. 验收标准

1. 玩家可以在休闲模式与闯关模式之间切换，两个模式的状态互不覆盖。
2. 刷新页面后，已解锁关卡和最佳成绩仍然存在。
3. 未完成前一关时，不能进入后续锁定关卡。
4. 每关至少有两条合理路线和两个常见收尾词。
5. 达到最大步数时正确进入失败状态。
6. 完成关卡后立即保存，并解锁下一关。
7. 使用提示会正确影响星级或辅助标记。
8. 切换中英文不会重置当前关卡进度。
9. 词典替换后可以批量验证全部关卡。
10. 手机端可以完成选关、游戏和结算的完整流程。

## 14. 当前版本决策

1. 保留原有 12 关并扩展到 100 关。
2. 保留三星评价 F13。
3. 删除章节体系 F17，关卡采用连续列表。
4. 保留最大步数与失败条件 F06。
5. 保留撤销最后一步 F21。
6. 删除失败三次后开放答案 F22。
