# 每日一环 · 好友挑战产品规格（MVP）

## 1. 目标与范围

每日一环用“每天同一道题 + 可分享成绩”的轻量玩法形成自然传播。MVP 先交付每日固定题、正式成绩、练习成绩、结算与挑战链接能力；全球排行榜、好友关系、付费奖励和强反作弊不在首版范围内。

核心漏斗为：进入每日题 → 完成词环 → 分享挑战 → 好友打开并完成 → 好友再次分享。

## 2. 每日题规则

- 日期边界固定为 `Asia/Shanghai`（北京时间），每天 00:00 换题。
- 题目从至少 30 道人工筛选题中按日期确定性轮换。同一北京时间日期在不同设备上必须得到相同题目。
- 每题包含 `id`、`title`、`startWord`、`difficulty`、`parMoves`、`maxMoves`、`hintLimit`。
- 题目复用已通过词图校验的闯关起始词；标准步数、最大步数与提示次数必须与对应闯关配置一致。
- 最大步数内闭环即完成；步数越少越好，步数相同则用时越短越好。
- 计时从玩家第一次提交有效单词开始，到闭环成功停止。页面仅打开、阅读规则或输入无效词不启动计时。

## 3. 成绩定义

- **今日首通**：玩家当天第一次完成的结果，一经写入不覆盖。
- **今日最佳**：按“步数优先、用时第二”更新；完全相同的成绩保持原记录。
- **正式成绩**：首次尝试过程中未重新开始、未载入已经看过答案的练习路线，且由正常游戏流程完成。
- **辅助完成**：使用提示、查看答案或撤销任一功能后完成。辅助完成可以更新个人记录，但必须带辅助标记，不能显示“无提示”文案。
- **练习成绩**：当日首通后的重复游玩，或往期题目的游玩结果。练习成绩不覆盖今日首通，但可以更新今日最佳。
- 好友胜负比较依次使用：完成状态、步数、用时，两项均相同时为平局。个人最佳优先保留无辅助成绩，再按步数和用时比较；辅助状态始终明确展示。

## 4. 中断、刷新与跨日

- 未完成局面按日期保存路线、已用步数、计时起点、累计用时和辅助状态；刷新后继续。
- 玩家主动“重新开始”会清空当局路线和计时，并把后续结果标记为练习成绩。
- 北京时间跨日后，不把前一天的未完成局面带入新题。若页面仍停留在旧题，提示“今日题目已更新”，允许完成旧题但按往期练习记录。
- 本地存储不可用时仍允许游玩；成绩只保留在当前页面，界面明确提示无法持久保存。
- 登录同步不得覆盖更优的本地最佳成绩；合并时保留最早首通和比较规则下的最佳成绩。

## 5. 挑战链接与隐私

- 分享链接只携带不可猜测的挑战码，不在查询参数中放置答案路线、邮箱、用户 ID 或设备标识。
- 挑战记录包含题目日期、发起人成绩、辅助标记和可选昵称。好友打开后先看到目标成绩，再进入相同题目。
- 当日挑战直接计入今日成绩；过期链接仍可玩原题，但标记为“往期挑战/练习成绩”。
- 无效或不存在的挑战码降级到当天普通每日题，并显示简短提示。
- 分享卡只展示日期、步数、用时、辅助状态和不泄露完整答案的双字母轨迹。

## 6. 游客、服务端与降级

- 游客无需注册即可游玩、保存本机成绩和分享；服务端仅使用随机匿名安装标识去重，不采集完整 IP 或设备指纹。
- 登录用户可同步成绩。游客登录后合并本地数据，不删除原本的闯关、成就或休闲模式存档。
- 服务端负责返回权威北京时间日期，并校验日期、题目、步数、用时及字段范围。
- API 或网络不可用时允许完成和保存本地成绩，隐藏在线人数、百分位和挑战码创建；恢复网络后可补交成绩。
- 首版没有奖品和公开排名，只做基本频率限制与重复提交保护，不承诺强反作弊。

## 7. 界面与文案

- 开始页入口显示“每日一环”、当天状态和距离换题的时间。
- 游戏页显示日期、题目编号、难度、步数、计时和提示余额。
- 结算页显示首通/练习、步数、用时、辅助状态、个人最佳以及与发起人的胜负结果。
- 主操作为“分享成绩”，次操作为“复制挑战链接”和“再玩一次”。
- 中文分享文案：`今日词环 #{number}｜{moves} 步闭环 · {time} · {assist}｜你能比我少一步吗？`
- 英文分享文案：`Daily Word Loop #{number} · {moves} moves · {time} · {assist}. Can you beat me?`
- 在线样本不足时仅显示“今天已有 N 人完成”；达到产品设定的最小样本量后才显示百分位。

## 8. 数据与验收指标

记录曝光、开始、完成、分享、分享链接打开、受邀者开始、受邀者完成、二次分享和次日回访事件。事件不得包含完整答案路线。

MVP 验收条件：

1. 同一北京时间日期在不同时区设备上选中同一题，00:00 准确换题。
2. 题池不少于 30 道，难度分布均衡，所有题均通过闯关词图校验。
3. 刷新可恢复未完成局面；跨日、本地存储失败和网络失败均有明确降级路径。
4. 首通、最佳、辅助、练习和好友胜负按本规格一致计算。
5. 分享内容不剧透完整路线，挑战链接不暴露个人敏感信息。
6. 自动检查覆盖题目字段、唯一性、范围、闯关配置一致性、北京时间边界和确定性轮换。
