# 双语词条功能事实记录

核验日期：2026-08-28

- 英文词条使用 Free Dictionary API：`https://api.dictionaryapi.dev/api/v2/entries/en/<word>`。
- 可用字段包含单词、音标、发音音频、词性和英文释义。
- Datamuse API 作为并行备用英文词典：`https://api.datamuse.com/words?sp=<word>&md=dpr`，可返回英文释义和发音标注；客户端只接受与请求单词完全匹配的结果。
- 中文释义由 MyMemory Translation API 直接翻译单词本身：`https://api.mymemory.translated.net/get?q=<word>&langpair=en|zh-CN`；不翻译整段英文解释。
- MyMemory 文档说明 `q` 参数上限为 500 字节，因此客户端会先截断过长释义。
- 2026-08-28 以 `embrace` 实测两个接口成功，并确认响应允许浏览器跨域访问。
- 这是依赖网络的免费公共服务，不保证覆盖本地 SCOWL 词库中的每个生僻词；未查到时界面必须明确显示“暂无在线释义”，不可补造解释。
- 两个英文词典并行查询，先返回有效精确词条的服务优先；单次请求 8 秒超时，避免界面无限等待。
- 中英文均查询成功的词条在浏览器本地缓存 30 天，减少重复请求和免费服务压力。
- 翻译结果必须经过 HTML 实体解码、标签/网址/控制字符过滤和中文字符校验；单词直译不可用时，才用英文解释作为上下文生成简短译词。仍无法得到可靠中文时显示缺省提示，不展示原样英文或异常内容。
